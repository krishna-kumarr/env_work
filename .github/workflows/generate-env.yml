name: Test Upload Artifact

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "test" > test.txt
      - uses: actions/upload-artifact@v3
        with:
          name: test-artifact
          path: test.txt
